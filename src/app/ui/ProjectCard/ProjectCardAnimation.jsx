// ProjectCardAnimation is now a harmless passthrough kept for compatibility.
// All animation logic has been moved into `ProjectCard.jsx`.
export default function ProjectCardAnimation({ children }) {
    return children;
}